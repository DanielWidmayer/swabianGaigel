<!-- <div style="width: 100%; height: 500px; background-color: lightgreen;">
    <div style="display: flex;">
        <div id="playfield" style="width: 70%;"><%- include roomparts/playfield.ejs %></div>
        <div style="width: 30%;">
            <div><%- include roomparts/userlist.ejs %></div>
            <div><%- include roomparts/chatfield.ejs %></div>
        </div>
    </div>
    
</div> -->
<div class="gamecontainer m-5">
    <div class="row">
        <div class="col-8">
            <div class="card fieldcontainer p-3">
                <img class="preload" src="/images/cards.png" />
                <div id="card-table">
                    <button class="btn btn-secondary" id="deal">DEAL</button>
                </div>

                <footer>
                    <p>room#18648</p> <button class="btn btn-secondary" type="button" id="bleave">Leave</button>
                </footer>
            </div>
        </div>
        <div class="col-4">
            <div class="card">
                <div id="game-history">
                    <h3>Spielverlauf</h3>
                </div>
            </div>

            <div class="card">
                <div id="chat">
                    <h3>Chat</h3>
                </div>
            </div>
        </div>

    </div>
</div>

<script>
    const bleave = $('#bleave');
    const hash = window.location.href.split('/').pop();

    io.socket.post('/socketconnect', function (res, jres) {
        if (jres.statusCode != 200) {
            console.log(res);
        } else {
            console.log("ok");
        }
    })

    bleave.click(socketleave);

    function socketleave() {
        io.socket.delete('/leave', function (res, jres) {
            if (jres.statusCode != 200) {
                console.log(res);
            } else {
                window.location.href = '/list';
            }
        })
    }

    // onunload event could be used to make the user leave without button, but would also cause him to leave on refresh
</script>